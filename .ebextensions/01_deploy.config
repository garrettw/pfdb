container_commands:
  00_fix_permissions:
    command: "chmod -R 775 storage bootstrap/cache && chown -R webapp:webapp storage bootstrap/cache"
  01_migrate:
    command: "bash .ebextensions/migrate.sh"
    leader_only: true
